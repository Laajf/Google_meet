# flake8: noqa

import builtins
import sys

from obrabotka_text import test

ModuleNotFoundError = getattr(builtins, 'ModuleNotFoundError', ImportError)

try:
    from obrabotka_text.test import check_warnings
except (ModuleNotFoundError, ImportError):
    from obrabotka_text.test import check_warnings


try:
    from obrabotka_text.test import (
        rmtree,
        EnvironmentVarGuard,
        unlink,
        skip_unless_symlink,
        temp_dir,
    )
except (ModuleNotFoundError, ImportError):
    from obrabotka_text.test import (
        rmtree,
        EnvironmentVarGuard,
        unlink,
        skip_unless_symlink,
        temp_dir,
    )


try:
    from obrabotka_text.test import (
        DirsOnSysPath,
        CleanImport,
    )
except (ModuleNotFoundError, ImportError):
    from obrabotka_text.test import (
        DirsOnSysPath,
        CleanImport,
    )


if sys.version_info < (3, 9):
    requires_zlib = lambda: test.support.requires_zlib
